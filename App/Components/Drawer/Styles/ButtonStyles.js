import { StyleSheet } from 'react-native';
import { Colors, Metrics } from '../../../Themes';

export const buttonColor = Colors.secondary;

const styles = StyleSheet.create({
  container: {
    padding: Metrics.basePadding
  }
});

export default styles;
